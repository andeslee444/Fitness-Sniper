{
  "installCommand": "npm ci --workspace=web --workspace=packages/shared --include-workspace-root",
  "buildCommand": "npm run web:build",
  "outputDirectory": "web/.next",
  "framework": "nextjs"
}
